import React from 'react';
import TodoItems from './TodoItems';

const Todos = props => {
    return (
        <div>
            {/* {props.todos.map(todo =>
                todo.description
            )} */}
            {/* <TodoItems /> */}
        </div>
    )
}

export default Todos
